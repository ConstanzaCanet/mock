
<h2 class='titleForm'>PRODUCTOS EN EL ALMACEN</h2>
{{#if products}}
{{#each products}}
<div class='cardTimeReal container'>
    <div class="imgCont">
        <img src={{this.body.thumbnail}} class='imgCard'>
    </div>

    <div class='bodyCard'>
        <h4>
            {{this.body.name}}
        </h4>
        <p>
            $ {{this.body.price}}
        </p>
    </div>
</div>
{{/each}}
{{/if}}
